"""Evaluation (Task 13): extraction accuracy, match/STP rate, retrieval metrics.

Metric functions are pure and unit-tested locally; the harness that runs the
real pipeline over the dataset lives in :mod:`.harness` (Docker).
"""
